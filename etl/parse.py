"""Reads stat.gov.az .xls files, normalizes to canonical rows.

Inspect each file before parsing: merged cells, multi-row headers, inline
methodological footnotes — the files are NOT uniform (PROJECT.md). Do not
assume structure.

Stub — implemented in build Phase 1.
"""


def main() -> None:
    raise NotImplementedError("parse.py: implemented in build Phase 1")


if __name__ == "__main__":
    main()
