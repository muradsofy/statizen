// Maps an indicator value to a fill on the accent luminance ramp.
// Stub — implemented in build Phase 5.

export function createColorScale(
  _min: number,
  _max: number,
): (value: number) => string {
  return (_value: number) => {
    throw new Error("createColorScale: not implemented (build Phase 5)");
  };
}
