// Mouse-driven parallax transform. Stub — build Phase 6.
// Will use framer-motion useMotionValue/useTransform; ≤30px; respects
// prefers-reduced-motion.

export function useParallax() {
  return { x: 0, y: 0 };
}
