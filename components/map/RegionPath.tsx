// One region's <path> + hover/active events. Stub — build Phase 2/3.

export interface RegionPathProps {
  regionId: string;
  d: string;
}

export function RegionPath(_props: RegionPathProps) {
  return null;
}
